Remove-Item -Path "C:\Users\输入你的win用户名\.rustup\downloads" -Force -Recurse -ErrorAction SilentlyContinue; New-Item -Path "C:\Users\输入你的win用户名\.rustup\downloads" -ItemType Directory -ErrorAction SilentlyContinue; echo "Cleaned downloads directory" 


如果使用官方安装包执行到最后因为权限问题 提示安装出错的话 ，修改上面的命令修改成自己的用户名 ，然后在pwsh中执行上面的命令，删除下载的数据以后。执行本文将下的 备用安装脚本：install-rustup-win.ps1